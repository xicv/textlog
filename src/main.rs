mod cli;
mod clipboard;
mod config;
mod error;
mod filters;
mod macos_perm;
mod mcp;
mod notifier;
mod ocr;
mod pipeline;
mod service;
mod storage;

fn main() {
    println!("textlog — clipboard + OCR daemon, MCP server for Claude Code");
}
